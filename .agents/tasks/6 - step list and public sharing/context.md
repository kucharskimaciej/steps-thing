# Step List And Public Sharing Context

The rewrite needs a dense private step list and a public step sharing route. Shortlinks use `{origin}/s/{stepId}`. Public step pages do not require auth but must be able to play the step's primary video.

